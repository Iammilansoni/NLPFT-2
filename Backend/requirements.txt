# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Database dependencies
motor==3.3.2  # Async MongoDB driver for database.py
pymongo==4.6.0  # MongoDB driver

# System monitoring (used in health.py)
psutil==5.9.6

# NLP dependencies
rapidfuzz==3.5.2  # For fuzzy string matching in rule engine

# Standard library imports (no additional packages needed for):
# - datetime, timezone
# - typing
# - asyncio
# - contextlib
# - logging

# Optional: Development and testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# Optional: Additional utilities (if needed later)
python-multipart==0.0.6  # For form uploads
aiofiles==23.2.1  # For async file operations
python-dotenv==1.0.0  # For environment variables
