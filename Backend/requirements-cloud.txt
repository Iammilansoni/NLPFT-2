# Cloud-mode extras — in-process inference, no Ollama container.
#
# Kept separate from requirements.txt so LOCAL mode does not pay ~250MB of
# onnxruntime it never uses, and so the two images diverge explicitly rather
# than by accident.

# In-process embeddings: bge-small-en-v1.5, 384-dim, ~130MB.
# Pulls onnxruntime, which flashrank also uses — one runtime, two models.
fastembed>=0.4.0

# pgvector SQLAlchemy types (Phase 3 storage backend).
pgvector>=0.3.0
